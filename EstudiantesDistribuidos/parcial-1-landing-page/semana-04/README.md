# Semana 4 (14-18 sep) (*) — Distribuidos

**Tipo:** Integradora

**Tema oficial:** U2·AE1 Protocolos de comunicación e interconexión de redes.

**Objetivo:** identificar los protocolos concretos que usa el stack del proyecto (HTTP/HTTPS, TCP, DNS) y su rol en la comunicación.

**Prerrequisitos:** semana 3.

**Actividad:** analizar con herramientas (pestaña Network del navegador, o `curl -v`) una petición real a la API de CECyT9, identificando las capas de protocolo involucradas.

**Entregable:** reporte "Protocolos identificados" con captura de `curl -v` anotada.

---
La rúbrica de esta práctica la tiene tu docente por separado.
